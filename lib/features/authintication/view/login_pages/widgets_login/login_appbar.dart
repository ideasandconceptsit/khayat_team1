import 'package:flutter/material.dart';

PreferredSizeWidget buildLoginAppBar() {
  return AppBar(
    backgroundColor: Colors.transparent,
    automaticallyImplyLeading: false,
    elevation: 0,
  );
}
